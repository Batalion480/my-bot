meta:
  version: 1.0
  name: my-bot
  toolchain:
    type: python
    version: 3.11

app:
  type: python

install:
  - pip install -r requirements.txt

run:
  - python main.py

settings:
  work_dir: /app
  port: 80